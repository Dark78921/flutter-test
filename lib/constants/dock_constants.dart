// A utility class containing constants for the Dock widget dimensions and styling
class DockConstants {
  // Base width of each dock item
  static const double baseWidth = 60.0;

  // Base height of each dock item
  static const double baseHeight = 55.0;

  // Spacing between each dock item
  static const double itemSpacing = 12.0;

  // Border radius for the entire dock container
  static const double borderRadius = 24.0;

  // Border radius for individual dock items
  static const double itemBorderRadius = 16.0;

  // Icon size for each dock item
  static const double iconSize = 26.0;
}
