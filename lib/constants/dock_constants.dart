/// A class that holds constant values used throughout the Dock widget.
class DockConstants {
  /// The base width of the dock items in pixels.
  static const double baseWidth = 60.0;

  /// The base height of the dock items in pixels.
  static const double baseHeight = 55.0;

  /// The spacing between dock items in pixels.
  static const double itemSpacing = 12.0;

  /// The border radius for the dock background in pixels.
  static const double borderRadius = 24.0;

  /// The border radius for individual dock items in pixels.
  static const double itemBorderRadius = 16.0;

  /// The size of the icons used in the dock items in pixels.
  static const double iconSize = 26.0;
}